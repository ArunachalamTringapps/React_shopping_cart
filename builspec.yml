version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing dependencies..."
      - npm install
  build:
    commands:
      - echo "Building Angular project..."
      - npm run build
  post_build:
    commands:
      - aws s3 sync build/ s3://s3bucket-react-app --delete